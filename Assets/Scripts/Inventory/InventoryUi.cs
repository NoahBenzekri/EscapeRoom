using UnityEngine;

public class InventoryUi : MonoBehaviour
{
    //todo assigns the inventory slots to the ui using sprite icons
    //todo hot bar and drag and drop
    //also make sure to update the ui when items are added or removed from the inventory
    

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
